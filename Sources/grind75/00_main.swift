@main
struct grind75 {
    static func main() {

        // on second thought, it's really easier to do this
        // with unit tests, isn't it?

        // print("Test case to run: ", terminator: "")
        // let input = readLine()
        // let parsed: Int? = Int(input!)
        // switch parsed {
        //     case 1: runTwoSum()
        //     case 2: isValidParens()
        //     case 3: mergeSortedLists()
        //     default:
        //         print("Invalid choice.")
        //         return
        // }
    }
}